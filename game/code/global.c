#include <menu.h>
#include <inter.h>
#include <stdio.h>

#define EMPTY -1

// Atribuições das estruturas de dados globais (struct):

gl_exclv exclv = {0, 0, 0, 0, 0, 0, 0, 0};
gl_level level = {EMPTY, LEVEL_1, LEVEL_1};
gl_score score = {0, 0, 0, 0, 0};
gl_check check = {EMPTY, EMPTY, EMPTY};
